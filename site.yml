---

- name: URI module
  hosts: all       
  become: yes                     
  
  tasks:
    - name: Check that you can connect (GET) to a page and it reuntres a status 200
      uri:
        url: http://www.example.com
        return_content: yes
      register: results

    - debug:
        var: results





